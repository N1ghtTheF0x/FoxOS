#ifndef COLORS_HPP
#define COLORS_HPP

#include "types.hpp"

enum struct Color
{
    Black = 0,
    Blue = 1,
    Green = 2,
    Cyan = 3,
    Red = 4,
    Magenta = 5,
    Brown = 6,
    LightGray = 7,
    DarkGray = 8,
    LightBlue = 9,
    LightGreen = 10,
    LightCyan = 11,
    LightRed = 12,
    LightMagenta = 13,
    LightBrown = 14,
    White = 15,
    Count
};

#define COLOR(fg,bg) ((u8)fg | (u8)bg << 4)

#endif